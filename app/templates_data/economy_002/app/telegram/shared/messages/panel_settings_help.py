"""English docstring for panel_settings_help."""

from __future__ import annotations

from typing import Any

from app.telegram.keyboards.common import glass_inline_button

PANEL_SETTINGS_HELP_PAGES: list[str] = [
    """📖 **راهنمای تنظیمات پنل** — صفحه ۱ از ۳

🏠 **ورود به این صفحه**
از منوی ادمین → مدیریت پنل‌ها → روی نام پنل بزنید. بالای صفحه مشخصات و وضعیت سرور است؛ پایین‌تر دکمه‌های تنظیمات را می‌بینید.

━━━━━━━━━━━━━━━━━━━━
🎨 **استایل دکمه (خرید / لیست)**
متن، رنگ (آبی/سبز/قرمز) و آیکون پریمیوم دکمه‌ای که کاربر هنگام **خرید سرویس** و **لیست وضعیت پنل‌ها** می‌بیند.
• فقط ظاهر است؛ روی عملکرد فنی پنل اثری ندارد.

⚡ **فعال‌سازی پنل** `(✅ / ❌)`
• **✅ فعال:** کاربران می‌توانند از این پنل کانفیگ بخرند و سرویس بگیرند.
• **❌ غیرفعال:** پنل در لیست خرید مخفی/غیرقابل استفاده می‌شود (سرویس‌های قبلی معمولاً باقی می‌مانند).

🔄 **ریستارت**
ری‌استارت سرویس پنل پاسارگارد/مرزبان از راه دور. برای اعمال بعضی تغییرات یا رفع گیر کردن مفید است.

📡 **نودها**
لیست نودهای متصل به پنل را نشان می‌دهد (وضعیت، ترافیک و …).

✏️ **نام**
تغییر نام نمایشی پنل در ربات (همان نامی که روی دکمه‌ها دیده می‌شود).

🔐 **ادمین**
مدیریت ادمین‌های پنل در خود پنل (افزودن/حذف از طریق API).

📦 **گروه پیش‌فرض**
گروه(های)ی که کاربر **جدید** هنگام ساخت کانفیگ به آن‌ها اختصاص می‌یابد.

👥 **سقف خرید سرویس از این پنل**
سقف تعداد کانفیگ روی این پنل (ربطی به ظرفیت کاربران نمایندگی ندارد).
• **نامحدود:** بدون سقف.
• **عدد:** مثلاً ۵۰۰ یعنی بیش از ۵۰۰ سرویس فعال روی این پنل ساخته نمی‌شود؛ پس از پر شدن، این پنل از بخش خرید سرویس مخفی می‌شود.
""",
    """📖 **راهنمای تنظیمات پنل** — صفحه ۲ از ۳

⏰ **خرید زمان** `(✅ / ❌)`
• **✅:** دکمه «تمدید زمان» / خرید پلن‌های زمانی برای کاربران این پنل فعال است.
• **❌:** فقط حجم (یا پلن‌های دیگر) قابل خرید است؛ مسیر تمدید زمان بسته می‌شود.

♾️ **خرید حجم** `(✅ / ❌)`
• **✅:** کاربر می‌تواند حجم اضافه یا پلن حجمی بخرد.
• **❌:** گزینه‌های مرتبط با حجم در فروش این پنل نمایش داده نمی‌شود.

📊 **نمایش پلن‌ها** — دو حالت:
• **📋 کلاسیک:** اول حجم/نوع پلن، بعد جزئیات (روش سنتی).
• **⏰ زمان‌محور (duration_first):** اول مدت (۷/۳۰/۹۰ روز …) انتخاب می‌شود؛ برای فروش بر اساس زمان مناسب‌تر است.

🔄 **تمدید خودکار** `(✅ / ❌)`
• **✅:** کاربر در «سرویس‌های من» می‌تواند تمدید خودکار را برای کانفیگ فعال کند (با انتخاب پلن و شرط زمان/حجم).
• **❌:** امکان تمدید خودکار در کل این پنل غیرفعال است.
⚠️ بعد از یک تمدید خودکار موفق، معمولاً دوباره باید توسط کاربر فعال شود.

📡 **اطلاع‌رسانی** — دو حالت:
• **🔔 وب‌هوک:** هشدار کم‌حجم / انقضا از **رویداد پنل** (مثلاً درصد مصرف در پاسارگارد) — سبک‌تر و دقیق‌تر برای آستانه درصد.
• **🤖 ربات (کرون):** ربات خودش دوره‌ای پنل را چک می‌کند (بدون اتکا به وب‌هوک پنل).

📊 **تمدید حجم** — بسیار مهم برای وب‌هوک:
• **➕ جمعی (پیش‌فرض):** حجم کل + حجم پلن؛ **مصرف ریست نمی‌شود**.
  مثال: ۲۰ گیگ کل، ۱۵ مصرف، تمدید ۵ → **۲۵ گیگ کل**، همان ۱۵ مصرف.
• **باقی+ریست:** حجم **باقیمانده** + حجم پلن؛ **مصرف صفر** می‌شود.
  همان مثال → **۱۰ گیگ کل** (۵ باقی + ۵ جدید)، مصرف از نو.
💡 اگر در پنل برای درصد مصرف وب‌هوک گذاشته‌اید، حالت **باقی+ریست** توصیه می‌شود.
""",
    """📖 **راهنمای تنظیمات پنل** — صفحه ۳ از ۳

🔗 **لینک ساب** — سه حالت (با هر کلیک عوض می‌شود):
• **اصلی + تانل:** هر دو لینک subscription نمایش داده می‌شود.
• **فقط اصلی:** فقط لینک IP اصلی پنل.
• **فقط تانل:** فقط لینک `tunnel_url` (تانل) اگر تنظیم شده باشد.

🔗 **لینک تکی**
کدام لینک‌های داخل سابسکرایبشن (ایندکس ۱، ۲، ۳ …) در پیام خرید/تست به کاربر نشان داده شود.
مثال: `1,2,3` یا `1-3` یا `all` — در متن‌های ربات از `{config_links}` استفاده می‌شود.

🌐 **پیشوند نود**
فیلتر و نمایش نام لوکیشن‌ها بر اساس پیشوند نود (مثل LT -, HYB -).
می‌توانی پیشوند سفارشی اضافه کنی و نمایش در لیست لوکیشن را روشن/خاموش کنی.

🧪 **تنظیمات تست**
حجم و مدت **اکانت تست** رایگان این پنل (گیگ و روز).

💸 **بازخرید کانفیگ**
• روشن: کاربر کانفیگ **فعال** را حذف کند و به ازای هر گیگ **باقیمانده** مبلغ به کیف پول برگردد.
• نرخ هر گیگ را جدا تنظیم می‌کنی (تومان).

🔁 **سینک آیدی کاربران**
در نسخه‌های جدید پاسارگارد، بسیاری از APIها به‌جای `username` با **آیدی عددی کاربر** (`id`) کار می‌کنند.
این دکمه لیست کاربران پنل را می‌خواند و برای هر کانفیگ موجود در دیتابیس ربات، فیلد **`panel_userid`** را ذخیره می‌کند تا درخواست‌های بعدی به پنل با آیدی عددی انجام شود (سریع‌تر و سازگار با API جدید).
• تطبیق بر اساس **نام کاربری** (`username`) است، نه آیدی تلگرام.
• کانفیگ‌هایی که در ربات نیستند یا در پنل پیدا نشوند «رد شده» حساب می‌شوند.
💡 بعد از مهاجرت پنل یا وقتی API با username خطا می‌دهد، یک‌بار این سینک را بزنید.

➕ **لینک تانل** / 🔄 **حذف لینک تانل**
• **تنظیم:** IP یا دامنه تانل ایران برای ساب جایگزین.
• **حذف:** برگشت به فقط لینک اصلی.

🔙 **لیست پنل‌ها**
بازگشت به فهرست همه پنل‌ها.

━━━━━━━━━━━━━━━━━━━━
💡 **نکته:** کنار هر دکمه وضعیت فعلی `(✅/❌/🔔/جمعی/…)` نشان داده می‌شود؛ با لمس همان دکمه حالت عوض می‌شود.
""",
]


def panel_settings_help_page_count() -> int:
    return len(PANEL_SETTINGS_HELP_PAGES)


def build_panel_settings_help_text(panel: Any, page: int) -> str:
    total = panel_settings_help_page_count()
    page = max(0, min(page, total - 1))
    body = PANEL_SETTINGS_HELP_PAGES[page]
    header = f"🧷 پنل: **{panel.name}** — `کد {panel.code}`\n\n"
    return header + body


def build_panel_settings_help_buttons(panel_code: int | str, page: int) -> list:
    total = panel_settings_help_page_count()
    page = max(0, min(page, total - 1))
    rows: list = []

    nav: list = []
    if page > 0:
        nav.append(glass_inline_button("◀️ صفحه قبل", data=f"panel_settings_help:{panel_code}:{page - 1}"))
    if page < total - 1:
        nav.append(glass_inline_button("صفحه بعد ▶️", data=f"panel_settings_help:{panel_code}:{page + 1}"))
    if nav:
        rows.append(nav)

    rows.append([glass_inline_button("🔙 بازگشت به تنظیمات پنل", data=f"panel_info:{panel_code}")])
    return rows
