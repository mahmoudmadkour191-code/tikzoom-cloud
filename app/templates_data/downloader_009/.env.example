BOT_TOKEN=123456:CHANGE_ME

# Local Bot API, e.g. http://127.0.0.1:8081
TELEGRAM_API_BASE_URL=http://127.0.0.1:8081
TELEGRAM_API_IS_LOCAL=true

MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=playdl
TOOLS_DIR=tools
DOWNLOAD_DIR=storage/downloads
MAX_PARALLEL_JOBS=2

# If true, bot downloads missing helper tools on startup.
# It will not install OS packages like Java, MongoDB, or git.
AUTO_INSTALL_TOOLS=true

# auto: prefer alltech gplay wrapper if present, then gplaydl, then apkeep.
# other values: alltech-gplay, gplaydl, apkeep, custom
PLAY_DOWNLOADER_BACKEND=auto
ALLTECH_GPLAY_PATH=tools/gplay-apk-downloader/gplay
ALLTECH_AUTO_AUTH=true
ALLTECH_AUTH_FILE=~/.gplay-auth.json
PLAY_ARCH=arm64
MERGE_SPLITS=true
APKEEP_SOURCE=
APKEEP_EMAIL=
APKEEP_TOKEN=

# Used only when PLAY_DOWNLOADER_BACKEND=custom.
# Template vars: {url} {package} {output_dir} {arch}
PLAY_DOWNLOADER_CMD=

APKEDITOR_JAR=tools/APKEditor.jar

# Optional override. Template vars: {input} {output}
APKS_TO_APK_CMD=

AUTO_SIGN_APK=true

# Optional post-merge signing. Template vars: {input} {output}
# Example:
# SIGN_APK_CMD=apksigner sign --ks /root/.android/debug.keystore --ks-pass pass:android --key-pass pass:android --out "{output}" "{input}"
SIGN_APK_CMD=

# daily limit for downloading from IR Uploader (currently just NixFile)
DAILY_LIMIT_IR=

# nixfile username and password, the bot use them to login and upload files
NIXFILE_USERNAME=
NIXFILE_PASS=

# If true: keep downloaded files on disk until DOWNLOADS_MAX_MB cap (sweeper clears).
# If false: delete files immediately after upload (telegram or nixfile).
KEEP_FILES=true

# rar binary used for the file-to-link flow. PATH name or absolute path.
RAR_PATH=rar

# Rubika delivery (secondary uploader, sends to user DM).
# Generate the session file once with:  python session_rubika.py
RUBIKA_SESSION_NAME=playdl_rubika
RUBIKA_SESSION_DIR=storage
RUBIKA_MAX_FILE_MB=500
RUBIKA_UPLOAD_TIMEOUT=900
