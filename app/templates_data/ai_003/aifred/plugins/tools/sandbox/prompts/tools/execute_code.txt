Execute Python code in a secure sandbox to perform calculations, data analysis, or generate visualizations. Use this tool when the user asks for: mathematical computations, statistical analysis, data processing, chart/plot generation, simulations, or any task that benefits from actual code execution rather than estimation. Available libraries: math, statistics, numpy, pandas, matplotlib, scipy, sklearn, seaborn, plotly. The sandbox has no network access and a 30-second time limit. Always print() results you want to return. For INTERACTIVE visualizations (3D plots, animations, dashboards): use plotly and save with fig.write_html("output.html", include_plotlyjs=True). For static plots: use matplotlib (images are captured automatically). For HTML/JS apps: write to "output.html". NEVER print a full HTML document to STDOUT — printed HTML is dropped; only the file write embeds it in the chat. IMPORTANT: When generating HTML with Canvas or visualizations, always use width:100% and height:100vh so the content fills the entire display area — never use fixed pixel sizes. Set body margin and padding to 0. HTML LIBRARIES: the render browser has NO external network — external CDNs are blocked and fail silently. Reference the locally mirrored libraries instead: Chart.js "{VENDOR_BASE}/chart.umd.min.js", D3 "{VENDOR_BASE}/d3.min.js", Plotly "{VENDOR_BASE}/plotly.min.js", Three.js "{VENDOR_BASE}/three.min.js". For Plotly from Python keep using fig.write_html("output.html", include_plotlyjs=True) which inlines it. If you need a library that is not mirrored, ask the user to add it (scripts/fetch_vendor_libs.sh) — never fall back to an external CDN. IMPORTANT: All plots and HTML outputs are automatically displayed in the chat. After calling this tool, do NOT attempt to display images yourself — no base64, no data URIs, no img tags. Just describe the result in text.

FILE ACCESS: The user's documents are available under the relative path "documents/" (e.g. open("documents/Polizei/foo.txt")) — READ-ONLY, attempts to open them in write mode will fail with PermissionError. If you need to persist output into the user's documents directory, use the separate `execute_code_write` tool (requires elevated permission). Otherwise write into the current directory (e.g. open("result.txt", "w")) — those files are ephemeral but can be returned via print() or HTML/image output.

HANDOFF: The working directory is DELETED after each run — a later execute_code call CANNOT read files from an earlier one, and the chat-embedded HTML/plot copies are display artifacts invisible to all file tools. Any deliverable that you, another agent, or the user needs to work with later (e.g. an app another agent will review or refactor) MUST be saved via `execute_code_write` to documents/<descriptive-name> — that is the only shared, persistent location. Files in documents/ can later be MODIFIED incrementally (read, patch, write back via execute_code_write) and rendered DIRECTLY via render_html ("/_upload/documents/<name>.html") — never copy them into the sandbox just to test them, and never regenerate a full file from memory when a targeted patch does the job.