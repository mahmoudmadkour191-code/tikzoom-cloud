# Changelog

All notable changes to PageFly are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- **Workspace — pending-suggestion review UI (AI-suggestion Phase 4).** New `frontend/src/components/workspace/SuggestionBar.tsx`: when a document has a pending AI suggestion, an amber bar shows the agent + a red(old)→green(new) diff with Accept / Reject buttons; a 409 surfaces a "text changed since the AI proposed this — reject and ask again" hint. `WorkspacePage` polls `GET …/suggestions` on document open and after every AI chat turn (the agent may have staged one via its tool); Accept reloads the document content + revision into the editor, Reject leaves it untouched, and pending state is cleared on create/delete/ingest. Closes the human-in-the-loop "AI proposes → human accepts/rejects" loop end to end. The in-editor inline range highlight is intentionally deferred (markdown-offset → ProseMirror-position mapping is a v2 refinement); the bar is the correct minimal review surface. Frontend has no JS test harness in this repo — backend Phases 0–3 are covered by 100 automated tests; this phase is verified by manual E2E.
- **Workspace — agent propose-suggestion tool (AI-suggestion Phase 3).** New `propose_workspace_suggestion` MCP tool (`doc_id`, `quote`, `replacement`, `reason`) registered in the knowledge tools server + allowlist alongside the existing read-only `read_workspace_document`. The agent can stage exactly one pending character-level suggestion per document for the human to accept/reject; it still cannot mutate document content directly (a test asserts the document is unchanged after a proposal). `AnchorNotFound` returns a recoverable message (refine the verbatim quote + retry, candidate count surfaced); an existing pending suggestion returns a clear "wait for the human" conflict message; the reason is echoed back so it reaches the reviewer. 5 tests, TDD.
- **Workspace — suggestion API endpoints (AI-suggestion Phase 2).** Three endpoints under `verify_token`: `POST /api/workspace/documents/{id}/suggestions` (create the single pending suggestion; `AnchorNotFound` → 422 with reason+candidates, second pending → 409 `PENDING_SUGGESTION_EXISTS`, replay with the same `Idempotency-Key` header returns the original row instead of 409), `GET …/suggestions` (the pending one or `null`), `POST …/suggestions/{sid}/resolve` (`accept` applies + bumps revision, `reject`/`cancel` leave the doc untouched, drift → 409). Adds an `idempotency_key` column + partial unique index to `workspace_suggestions`; the index is created in the migration block (not the schema string) and unconditionally so a stale suggestion table can't make `executescript` abort and skip later tables. 9 API tests, TDD.
- **Workspace — character-level suggestion core (AI-suggestion Phase 1).** New `src/workspace/anchor.py` implements the normative quote-anchor algorithm G (exact match → prefix/suffix narrowing → `occurrence` pick → unique resolve → capped ambiguous-candidates error; opt-in whitespace-flexible fuzzy that keeps offsets exact). New `workspace_suggestions` table with a SQLite partial unique index (`WHERE status='pending'`) that enforces the one-pending-suggestion-per-document rule at the DB layer, plus create/get/resolve helpers. New `src/workspace/suggestions.py` service ties anchor + storage + markdown: `create_pending` resolves the quote against canonical `content_md` and stores the range+hash; `resolve(accept)` RE-resolves on the current markdown so unrelated edits that shift offsets still work, refuses with `SuggestionDrifted` if the anchored text itself changed (document never mutated at a wrong location), applies the replacement, and bumps revision under the existing optimistic lock; `resolve(reject)` leaves the document untouched. 20 tests, TDD.
- **Workspace — canonical Markdown storage groundwork (AI-suggestion Phase 0).** Workspace documents now persist a derived `content_md` alongside the editor's `content` HTML. New `src/shared/md.py` (`html_to_markdown` / `markdown_to_html`, built on the existing `markdownify` + `markdown` deps) replaces the lossy inline regex that silently dropped tables, images and links — verified by a gate test covering the Tiptap toolbar's full feature set (headings, marks, ordered/bullet lists, quote, code, table, image, link). `content_md` is derived in the DB write path (`insert_workspace_document` / `update_workspace_document`) so HTML and Markdown can never diverge; a migration adds the column and backfills it from existing rows' HTML. This is the anchor-truth foundation for the upcoming character-level AI suggestion flow (comments/suggestions resolve against Markdown, not HTML).

### Fixed
- **Roam — truncated article previews gave no visual cut indicator.** `format_roam_message` capped a preview to `max_preview` chars and appended the "(full article…)" note, but no ellipsis, so a hard-cut sentence looked like the whole preview. Now appends `...` before the note when the source was longer than the cap.
- **Desktop — Unread-count badges in window titles fragmented dedup into one row per incoming message.** Bloome and similar chat apps title their window `"(3) Bloome"`, `"(8) Bloome"`, `"(22) Bloome"` etc. — the parenthesized number is the unread count. `ContextHash.compute` included `windowTitle` raw, so each tick of that count produced a fresh hash and a fresh row, even when nothing else changed: 3h sample showed 35+26+22+21+18+17+15+… rows for the same chat — all carrying nearly identical text excerpts that the user noticed as "this paragraph appears many times in the database". Fix: new `ContextHash.stripBadgeCounts(_:)` peels leading `(N)` / `[N]` / `(99+)` patterns off the title before hashing (recursively, so `"(3) (Beta) Slack"` → `"(Beta) Slack"`); the snapshot itself keeps the original title so the dashboard still shows the badge. Same-chat dwell now collapses into one row whose `duration_s` grows across message arrivals; switching chats still rotates to a new row because the URL changes.
- **Desktop — Empty-context rows and Electron helper subprocesses cluttered the capture stream.** Diversity audit of the last 3h showed 136/864 rows (15.7%) with both `text_excerpt=""` and `url=""` — transient AX states mid-navigation, focus on a bare AXGroup, and Skia apps between OCR rescues that render as "No text captured for this row" on the dashboard. Additionally ~5% of rows came from Electron helper processes (`com.electron.lark.helper`, `com.google.Chrome.helper.*`) that briefly win frontmost during IME handoff or GPU IPC but carry nothing. Two drops in `PrivacyFilter.sanitize`: (1) bundle IDs containing `.helper` are skipped; (2) rows with simultaneously empty text AND empty URL are dropped. Rows with a URL but no text survive — a naked URL is still work-log context ("user was on bloome.im at 14:02"). Also changed `AXReader` text source selection from "always prefer focused element" to "whichever is longer" — a short placeholder like Feishu's 16-char "发送给 Bloome" used to beat a 1000-char harvested window walk because focused text always won; now the window walk wins when it has more substance, so chat apps with empty input fields still capture their visible message log.
- **Desktop — Sidebar chrome and Electron class names dominated captures for some apps.** Diversity ratio (unique text-prefixes ÷ event count over the last few hours) flagged Maestri at 0.12 (33 identical "Maestri 边栏 已固定 …" prefixes) and Feishu at 0.24 (17 events starting with `ContentsView ClientView MainWidgetDelegateView TabContensView MultiWebView` — native view-class names bubbling up through `AXDescription`). The AX walk never distinguished a sidebar container from the main pane, so when sidebar text volume exceeded the main pane's, captures became a sidebar recap. Two narrow filters in `AXReader`: (1) **Sidebar-container skip** — when an element's `AXSubrole == AXSideBar` (Finder, Mail, Xcode, some native-wrapped Electron apps) or its `AXTitle` / `AXDescription` / `AXRoleDescription` lowercased matches a multi-lingual label set (`sidebar`, `navigation`, `nav`, `navigator`, `边栏`, `侧边栏`, `导航`, `导航栏`, `サイドバー`, `ナビゲーション`, `사이드바`, `barre latérale`, `seitenleiste`, `barra lateral`), the entire subtree is pruned like decorative roles; matching on the CONTAINER not the text means legitimate mentions of "sidebar" inside main content are unaffected. (2) **Electron view-class filter** — `TextCollector.add` rejects any string whose whitespace-separated tokens ALL are CamelCase alphabetic identifiers ending in `View`/`Widget`/`Delegate`/`Panel`/`Controller`/`Host`/`Container`/`Manager` (requires ≥2 tokens so ordinary words like "Preview" pass); kills the `ContentsView ClientView …` leak and similar Qt/Electron debug strings without touching real prose. Both filters layer on top of the existing decorative-role pruning and dedup — they subtract noise only, never overcounting.

### Changed
- **Desktop — API token storage moved from Keychain to a 0600 file** (`~/Library/Application Support/PageFly/api_token`). Keychain ACLs are bound to the binary's cdhash, and ad-hoc rebuilds change cdhash on every build, so the prior design popped a "PageFly Capture wants to use confidential information stored in your keychain" dialog after every reinstall. The new `TokenStore` is a single `Data.write(.atomic)` + `chmod 600`; trades Keychain's per-binary ACL for POSIX home-directory protection — the right tradeoff for a personal ad-hoc build, and the surface (`SettingsStore.apiToken` getter, `setApiToken(_:)`) is unchanged so callers don't notice. `probeToken()` falls back to a one-time Keychain read on first launch after upgrading; if it finds a legacy token it migrates the value into the file and deletes the Keychain entry, so subsequent rebuilds never touch Keychain again. A future Developer ID-signed release can swap back to Keychain (stable cdhash → no prompts).

### Fixed
- **Desktop — Browser captures polluted by tab strip chrome**. Chromium-based browsers (Comet, Chrome, Edge) expose their tab strip in the same AX tree as the page content — "Inactive tab · 186 MB freed up" per tab × 8 tabs dominated the 1500-node budget before the walk reached the `AXWebArea`. AXReader now does a shallow BFS (≤80 nodes) under the focused window looking for the first `AXWebArea`; if found, the main walk starts from there and skips toolbar/tab-strip/bookmarks entirely. Page URL also tried via `kAXURLAttribute` on that node. Fallback to whole-window walk if no `AXWebArea` is found (native apps). Text harvested from a 10+ tab Comet window went from ~20% page content / 80% chrome to ~95% page content.
- **Desktop — Canvas-rendered web pages showed empty text despite having a URL**. Feishu docs, Google Docs' canvas beta, Figma web, Linear's editor all render the document body on a `<canvas>` element that produces zero AX nodes. Auto-OCR trigger previously required both empty text *and* empty URL, which missed this case. Loosened to "text < 40 chars" regardless of URL; if a URL is present, OCR throttle key switches from `bundleID` → `bundleID + host + path` so each Canvas page gets its own 5-min cooldown (one Feishu OCR no longer locks the whole browser out for 5 min).

### Added
- **Desktop — Auto OCR for AX-blind apps (5-minute throttle)**. `CapturePipeline.sampleNow` now calls `OCRRescue.autoRescueIfEligible(bundleID:)` whenever an AX snapshot came back with empty `textExcerpt` AND empty `url` — the signature of Skia/custom-renderer apps (WeChat, Feishu, DingTalk, Figma desktop, games). Per-bundle TTL of 5 min means a persistent chat like WeChat gets one OCR snapshot every 5 minutes rather than once every sample (~6s) — average CPU ~0.07%, far below the ~15% continuous of rem/screenpipe. Silent on success (just logs, doesn't touch popover status); on permission-like failures, per-bundle TTL is bumped to 30 min to avoid spamming the log with the same "no Screen Recording TCC" every 5 min. Manual OCR button is unaffected — it ignores the throttle entirely so a user's explicit click always runs.

### Added
- **Desktop — "OCR window" rescue for AX-blind apps**. Some apps (WeChat 4.x `com.tencent.xinWeChat`, Feishu, DingTalk, QQ, Figma desktop) render everything on CALayers via Skia or a custom engine and expose *nothing* useful via the AX tree — my capture on those windows returns `window_title=<app name>` and empty `text_excerpt`. Rather than polling OCR globally (rem / screenpipe style, ~15% continuous CPU), new `OCR window` button in the popover runs a one-shot, on-device, user-triggered rescue: `WindowOCR` captures the currently focused window via `CGWindowListCreateImage` (macOS 13 compatible — `SCScreenshotManager` is 14+), runs `VNRecognizeTextRequest` with `recognitionLevel=.accurate` and languages `["zh-Hans","zh-Hant","en-US"]`, and `OCRRescue` inserts the result into `local_events` as a standalone row with `ax_role="AXOCR"` + a dedicated context hash (so the AX dedup path never tries to extend it) then kicks the uploader. Status surfaces in the popover via `@Published` props on `OCRRescue.shared` (n lines captured / error / clearing after 6s). Reuses the existing Screen Recording TCC grant; `NSScreenCaptureUsageDescription` wording updated in `project.yml` to cover both the audio-tap and OCR use cases. Refuses to OCR its own popover (guardrails against `bundleIdentifier == self`). No server-side changes — the existing `/api/activity/events/batch` endpoint accepts `AXOCR`-role rows unchanged.

### Added
- **Desktop — Pause/Resume capture from the menu bar**. New Pause button in the popover (next to Record / Test / Dashboard) flips `CapturePipeline.isPausedByUser`, which short-circuits `sampleNow` without tearing down workspace observers or the sampler timer — resume is instant. Header subtitle changes to "Paused", body text explains that uploads of already-queued events continue in the background, and the menu bar icon tint turns `systemYellow` via a new `.paused` state in `MenuBarController`. Pause is intentionally ephemeral (no UserDefaults) so quitting + relaunching always restores capture — prevents the "I paused last Tuesday and forgot" failure mode.
- **Desktop — Deeper AX capture for Electron / browser apps**. Walk budgets bumped (`maxNodes` 400→1500, `maxDepth` 12→20, `maxTextChars` 1500→4000). Three Screenpipe-derived improvements on top of the budget changes: (1) **Chromium tree materialization** — `AXEnhancedUserInterface=true` + `AXManualAccessibility=true` set on the app element per pid (TTL 180s to avoid re-triggering the expensive tree rebuild each poll); without these flags Chromium-based apps (Cursor, Slack, Notion, Discord, VSCode, Obsidian) return a stripped AX tree. Refs Chromium codereview.chromium.org/6909013, electron/electron#7206, obsidianmd#3002. (2) **Decorative role skip** — whole subtrees rooted at `AXScrollBar/AXImage/AXSplitter/AXGrowArea/AXMenuBar/AXMenu/AXToolbar/AXRuler/AXBusyIndicator/AXProgressIndicator/AXLayoutItem` are pruned so node budget reaches the actual content pane in Electron chrome-heavy UIs. (3) **Wall-clock budget** — walk gets a 300ms `deadline` on top of the node/depth caps; a pathological slow app can no longer burn `maxNodes × per-element timeout` seconds on the main thread. Per-element messaging timeout tightened 0.8s → 0.3s since wall-clock now backs it. Text collection also reads `kAXSelectedTextAttribute` (many Electron editors only expose text via selection). Reverted the `kAXVisibleChildrenAttribute` fallback from the previous iteration — cost an extra IPC round-trip per element and Electron apps routinely return `[]` for visible-children, net-negative; Screenpipe also walks `kAXChildrenAttribute` directly. Browser URL now tries `AXDocument` on the window first before descending into `AXWebArea` — Safari/Chrome/Edge expose it without needing the web tree walk.

### Fixed
- **Web — Pending capture rows had no way to see full content on small days.** The inline `Recent rows` list in `PendingDayCard` truncated text with `line-clamp-2` and was not clickable, and the `View all N events` modal button was gated on `event_count > samples.length`. That meant days with `event_count ≤ samples` (12 by default from `/api/activity/pending`) rendered a dead-end truncated list with no escape hatch. Fix: (1) new `InlineSampleRow` — same click-to-expand pattern as `DetailRow`, shows full `text_excerpt` in a `<pre>` with `max-h-240px overflow-y-auto` plus the URL as an external link; (2) modal button always surfaces when `event_count > 0` ("View all N" when there are more events than samples, "Open in full view" otherwise) so the scrollable-modal path is never gated off.

### Fixed
- **Desktop — Dashboard events sometimes showed row 1 as newer than row 0** on rapid app switches. `fetchRecentEvents` used `ORDER BY COALESCE(ended_at, started_at) DESC` with no tie-break, so during the few-millisecond window where a just-closed row's final `ended_at` equals (or narrowly exceeds) a brand-new live row's `started_at`, non-deterministic row order would put the older row on top. Added `started_at DESC, created_at DESC` as tie-breakers.

### Added
- **Desktop — AXReader now harvests page text + URL**. Previous build only read the focused element's `kAXValueAttribute` / `kAXSelectedTextAttribute`, which was nil for browser windows, chat windows, and most container UIs — so `text_excerpt` was empty for nearly every captured event. AXReader now bounded-walks the focused window (depth ≤ 12, ≤ 400 nodes, 0.4s per-element AX messaging timeout to bound main-thread cost on huge browser DOMs) and collects `kAXValue` / `kAXTitle` / `kAXDescription` from descendants, plus the URL from the first `AXWebArea` it encounters. Persisted text still prefers the focused element when present (active typing context wins) and falls back to harvested page text. Walk skips subtrees whose role is in `PrivacyFilter.blockedAXRoles`.
- **Desktop — Dashboard pagination**. Events list now pages through `local_events` instead of capping at the newest 250: 100 rows per page with prev/next buttons + `Page X of Y` indicator next to the section header. New `LocalDB.eventCount()` drives the counter; `fetchRecentEvents` gained an `offset` parameter. Page index is clamped on every refresh so deleting the only row on page N falls back to page N-1 instead of rendering an empty list.
- **Desktop — Dashboard window**. New "Dashboard…" button in the popover opens a standalone window (resizable, 760×600 default). Top fixed-height (240px) section lists every recording most-recent first with an inline play/stop button (`AVAudioPlayer` via `AudioPreview` singleton), duration, status, age, and a transcript preview when available. Bottom scrollable section shows the 250 newest captured events with app/window/URL/text excerpt. Both lists expose a 2-step "trash → Confirm/Cancel" delete only on rows the server hasn't accepted yet (`local_audio.status IN ('pending_upload','failed')` and `local_events.status = 'pending'`); deleting an audio row also removes its m4a from disk and stops playback if it was the active preview. Auto-refreshes every 4s while the window is open. Backed by new `LocalDB.fetchRecentEvents`, `deletePendingEvent`, `deleteLocalAudio`, `fetchAudio` helpers — each delete re-checks status under the write lock so a row that flipped to `uploading` between render and click is left alone with a user-visible "already uploading" message.
- **Desktop (M8) — menu bar UX polish + dashboard + audio retention**. Status icon is now a 6-state machine (noConfig / noPermission / offline / checking / armed / recording) with baked-in tints (`labelColor` for neutral, system semantic colors for active states) so it reads correctly on both light and dark menu bars; popover gains an Accessibility-permission banner with a single "Grant…" button that focuses the right System Settings pane and force-refreshes a shared `AXTrustMonitor`. New popover **Dashboard** section: live counts of queued events + audio waiting to upload, plus the last 5 recordings with status (recording / waiting / transcribing / transcribed / failed) and age. Recording state shows a `TimelineView`-driven elapsed timer. New audio retention slider in Preferences (0–50, default 5) — `AudioUploader.vacuumExpiredFiles()` enforces the keep-N cap on every flush in addition to the existing 7-day time cleanup; transcripts are kept on the server regardless. App icon (`AppIcon.appiconset`) and menu bar template (`MenuBarIcon.imageset`) generated procedurally via `desktop-capture/scripts/generate-icons.py`. New `desktop-capture/scripts/package-local.sh` produces an ad-hoc-signed DMG for personal use without an Apple Developer account.

### Fixed
- **Desktop — uploads stalling at 405**. Default server URL was `https://pagefly.ink`, which now serves the static landing-page SPA and rejects POST. Changed default to `https://api.pagefly.ink`; added a one-shot migration in `SettingsStore.init` so existing installs on the legacy default get rewritten silently.
- **Desktop — `prepareToRecord` failed under hardened runtime**. `package-local.sh` was re-signing with `codesign --force --deep --sign -` (no `--entitlements`), which silently stripped the microphone / audio-input entitlements and left the hardened runtime blocking `AVAudioRecorder`. Re-sign now passes `-o runtime --entitlements Resources/PageflyCapture.entitlements` and the script aborts if the microphone entitlement is missing post-sign.
- **Desktop — main-thread hang at launch on rebuilt ad-hoc binaries**. `SettingsStore` lazy init was calling `Keychain.load` on the main thread; under ad-hoc signing, `securityd` would block on an access-control prompt that LSUIElement apps can never surface, freezing the menu bar before the popover existed. Init no longer touches Keychain; `probeToken()` runs the read on a detached `Task` and publishes `hasToken` when it returns.

- **Desktop (M7) — launch-at-login + in-app updater + release script**. `SMAppService.mainApp` wired behind a Preferences → General "Launch at login" toggle; surfaces "requires approval" when macOS asks the user to flip the switch in System Settings → Login Items. New `UpdateChecker` polls the GitHub Releases API every 6h, compares against `CFBundleShortVersionString`, and exposes a "Check for Updates" / "Download" button in the About tab (opens the release HTML page in the browser — no auto-install). Hardened the URL path: repo slug is regex-validated (tamper-resistant against `defaults write`) and `html_url` host must live under `github.com`. New `desktop-capture/scripts/release.sh` wraps `xcodegen` + `xcodebuild archive` + `codesign` + `xcrun notarytool submit --wait` + `stapler staple`; export plist is built via `plutil -insert` so Developer ID identities with `&`/`<`/`>` don't corrupt the XML.
- **Desktop auto-capture — server side**. New `activity_events` + `audio_recordings` tables; helpers are idempotent on client-issued `local_uuid`. New endpoints under `/api/activity/*`: multipart audio upload with async Whisper transcription (atomic O_EXCL write, path-traversal-safe uuid validation), transcription status polling, batch event ingest (max 1000 events, FK-safe — unknown `audio_id` references are dropped to NULL with a warning), and event retrieval by date range. Canonical append-only log at `data/activity/YYYY-MM-DD.jsonl`, raw audio at `data/activity/audio/<uuid>.<ext>`. New `activity_log` agent summarizes a day's events + transcripts into a "Work log YYYY-MM-DD" wiki article; scheduled daily at 08:00 (registered in `/api/schedules` system list).
- **Scheduled task execution history** — every scheduled task run (system and user) is now recorded to the `task_runs` table with status (running/success/failed), started/finished timestamps, output, error, duration, and source (cron vs. user-triggered).
- **Run Now button** on user schedule cards — trigger a task immediately via `POST /api/schedules/{task_id}/run-now`; runs asynchronously and appears in history when complete.
- **Expandable run history** on the Schedules panel — click a schedule to view its recent runs; click a run to open a modal with full output / error and metadata.
- **`ingest` task type** implemented — dispatches to the query agent (same brain as the Telegram bot), so scheduled ingest/custom tasks can use all MCP tools (read/write knowledge, search, ingest URLs, compile wiki, etc.).
- New API endpoints: `GET /api/schedules/{task_id}/runs`, `GET /api/schedule-runs/recent`, `GET /api/schedule-runs/{run_id}`, `POST /api/schedules/{task_id}/run-now`.
- Telegram notifications now include a `Run #{id}` reference and truncate long output (3500 char cap) with a pointer to the Schedules tab for the full output.

### Changed
- Unified `custom` and `ingest` scheduled task types — both delegate to `src/agents/query.py:ask()`; the stored `task_type` still drives filtering and colors in the UI.
- Scheduler wraps every task in `_run_with_recording` — task failures never crash the scheduler, DB write failures log warnings but don't fail the task, notify failures are swallowed.
