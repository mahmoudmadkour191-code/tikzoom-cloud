Run the VLM on ALREADY-captured image(s) — does NOT take a photo. Works on ANY image_url from this conversation (vision_snapshot results or uploads): saved images stay accessible, so to RE-EXAMINE an earlier shot — more detail, a follow-up question, a second look the user asked for — call this again with the SAME url and a sharper, more specific prompt instead of taking a new photo. Pass urls EXACTLY as returned (they start with '/_upload/'). A list of urls is analysed as a temporal sequence (motion/'film'). Also pass `source_id` (from the snapshot result) so the per-camera briefing is applied. Workflow: vision_snapshot first, then vision_analyze on its image_url(s).
