services:
  bot:
    build: .
    env_file: .env
    environment:
      DB_PATH: /data/pumpguard.db
    volumes:
      - pumpguard-data:/data
    restart: unless-stopped
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    env_file: .env
    environment:
      DB_PATH: /data/pumpguard.db
      DASHBOARD_PORT: ${DASHBOARD_PORT:-8000}
    ports:
      - "${DASHBOARD_PORT:-8000}:${DASHBOARD_PORT:-8000}"
    volumes:
      # OAuth callback writes encrypted tokens; analytics routes still use SQLite mode=ro.
      - pumpguard-data:/data
    depends_on:
      - bot
    restart: unless-stopped

volumes:
  pumpguard-data:
