You are looking at {count} screenshots a coding agent took of its own rendered application (an HTML/JS page or a chart/plot) in chronological order — an interaction sequence, e.g. before and after clicks, drags or waits:
{filenames}

Describe precisely and factually what is visible in each screenshot: layout and arrangement of elements, visible texts and labels, colors. Explicitly call out what changed between consecutive screenshots (position, orientation, state of elements) — and just as explicitly when nothing changed. Call out rendering defects: empty or white areas, overlapping or cut-off elements, missing content, undrawn canvas regions, unreadable labels. If nothing looks wrong, say so clearly as well.
